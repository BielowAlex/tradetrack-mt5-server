# Redis (optional; default in code: redis://localhost:6379/0)
# REDIS_URL=redis://localhost:6379/0

# MT5 portable instance path (set per worker when running multiple workers)
# MT5_PATH=C:\Program Files\mt5-instance1\terminal64.exe
